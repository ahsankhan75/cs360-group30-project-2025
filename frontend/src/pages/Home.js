import React from "react";

const Home = () => {
  return (
    <div style={{ textAlign: "center", marginTop: "50px", fontSize: "24px" }}>
      <h1>Dashboard (Only accessible to authorized users)</h1>
    </div>
  );
};

export default Home;